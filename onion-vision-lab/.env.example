# Optional: override the vision API base URL. Default: /vision-api (Vite proxy -> localhost:8788).
# If unset/changed to an unreachable host, the app degrades gracefully to the
# in-browser HSV heuristic DEMO engine and labels itself DEMO.
VITE_VISION_API=/vision-api
