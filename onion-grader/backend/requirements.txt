# --- Runtime dependencies (full prototype) ---
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pillow>=10.3
opencv-python-headless>=4.10     # Layer A — image processing
numpy>=1.26
pyyaml>=6.0                      # grading_rules.yaml
reportlab>=4.0                   # Phase 10 — PDF reports
scikit-learn>=1.4                # Phase 5 baseline ML + Phase 13 metrics

# --- Testing ---
pytest>=8.0
httpx>=0.27

# --- Later (deployment choices) ---
# sqlalchemy>=2.0 / supabase      # Phase 11 production DB (SQLite used now)
# torch>=2.2, ultralytics>=8.2    # Phase 5 advanced: CNN + YOLO (train in Colab)
